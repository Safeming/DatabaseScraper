# MySQL 数据库连接配置 (复制为 .env 后填入实际值)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=ai_scraper_db

# 只读账号 (用于 SQL 查询页面，避免误操作)
DB_RO_USER=ai_scraper_ro
DB_RO_PASSWORD=readonly_pass

# 现有 LLM API 配置
API_KEY=
